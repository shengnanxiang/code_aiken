# AiKen 传输状态详细设计

> 本文档对 AiKen 与手机、口袋打印机的所有传输相关状态进行详细定义，补充《AiKen软件功能说明书》3.1.2 交互设计中未覆盖的传输边缘状态。

---

## 一、设计原则

1. **传输是瞬态**：传输中打断稳态，传输结束后回到稳态。
2. **手机传输 vs 打印机传输的关键区别**：
   - 手机传输：传输成功后**删除相机内照片**并重置计数器为 36。因此传输中**禁止过片和拍摄**，防止数据竞争。
   - 打印机传输：仅打印最后一张照片，**不影响相机内存储**。因此传输中**允许过片和拍摄**。
3. **失败不丢数据**：任何传输中断或失败，**不删除相机内照片**，用户可重新发起传输。
4. **失败有反馈**：传输失败后，设备给出短暂的可感知提示（橙色闪烁 3 秒），然后回到稳态，不会卡在错误界面。
5. **低电量不阻断传输**：低电量稳态下触发传输，传输瞬态优先显示。但电量到达临界值时强制中止传输以防止数据损坏。

---

## 二、与手机传输

### 2.1 传输中（瞬态）

| 项目 | 定义 |
|---|---|
| **设备状态** | 与手机连接，传输中 |
| **屏幕** | 滚动 |
| **指示灯** | 白色，闪烁 |
| **说明** | 不过片、不可拍摄。传输完成后将删除相机内全部照片并重置计数器。 |
| **触发条件** | 手机端发起"取出"传图命令，设备已与手机蓝牙连接。 |
| **结束条件** | 全部照片传输完成 → 进入 2.2 传输成功；连接断开或出错 → 进入 2.3 传输中断/失败。 |

### 2.2 传输成功（瞬态 → 稳态过渡）

| 项目 | 定义 |
|---|---|
| **设备状态** | 手机传输成功 |
| **屏幕** | `36`（计数器重置，常亮 2 秒后回到正常显示） |
| **指示灯** | 白色，常亮（2 秒后回到稳态灯效） |
| **说明** | 相机内照片已全部删除，计数器重置为 36。可过片、可拍摄。 |
| **触发条件** | 全部照片传输完成且手机端确认接收。 |
| **结束后** | 回到"与手机连接，待机"稳态（白色常亮）。若手机已断开，回到"无连接，正常待机"稳态。 |

> **设计说明**：传输成功后屏幕显示 `36` 常亮 2 秒，给用户一个明确的"完成"信号——计数器从之前的数字跳回 36，用户立刻知道"这卷冲扫完了，新的一卷开始了"。2 秒后回到正常待机显示。

### 2.3 传输中断/失败（瞬态 → 稳态过渡）

| 项目 | 定义 |
|---|---|
| **设备状态** | 手机传输中断/失败 |
| **屏幕** | 剩余张数，闪烁 3 秒 |
| **指示灯** | 橙色，闪烁 3 秒 |
| **说明** | 传输未完成，**相机内照片不删除**，计数器不重置。用户可在手机端重新发起"取出"传输，已传输的照片同名覆盖。3 秒后回到稳态。 |
| **触发条件** | 传输过程中蓝牙连接断开、手机端 App 异常退出、或传输超时（如 60 秒无数据响应）。 |
| **结束后** | 若手机仍连接，回到"与手机连接，待机"稳态（白色常亮）。若手机已断开，回到"无连接，正常待机"稳态（白色常亮）。 |

> **设计说明**：
> - 屏幕显示**剩余张数闪烁**（而非 `EE`），因为这不是设备故障，是传输未完成。剩余张数闪烁让用户知道"照片还在，只是没传完"。
> - 3 秒提示后静默回到稳态，不强制用户做任何操作。用户可以稍后重新发起传输。
> - 与 State 11（无照片时打印）的"数字闪烁 + 橙色闪烁 3 秒"模式一致，保持统一的"操作被拒绝/未完成"反馈语言。

---

## 三、与打印机传输

### 3.1 传输中（瞬态）

| 项目 | 定义 |
|---|---|
| **设备状态** | 与打印机连接，传输中 |
| **屏幕** | 滚动 |
| **指示灯** | 蓝色，闪烁 |
| **说明** | 可过片、可拍摄。仅传输最后一张照片至打印机，不影响相机内存储状态。 |
| **触发条件** | 设备已与口袋打印机配对连接，且相机内至少有 1 张照片时，短按"印放"键。 |
| **结束条件** | 照片传输完成且打印机开始打印 → 进入 3.2 传输成功；连接断开或出错 → 进入 3.3 传输中断/失败。 |

> **与手机传输的区别**：打印机传输中允许过片和拍摄，因为打印只读取最后一张照片的副本，不涉及删除和计数器重置。

### 3.2 传输成功（瞬态 → 稳态过渡）

| 项目 | 定义 |
|---|---|
| **设备状态** | 打印机传输成功 |
| **屏幕** | 剩余张数，常亮（恢复正常显示） |
| **指示灯** | 蓝色，常亮 |
| **说明** | 照片已传输至打印机，打印机开始/已完成打印。相机内照片不受影响，可继续过片、拍摄。 |
| **触发条件** | 照片传输完成，打印机端确认接收。 |
| **结束后** | 回到"与打印机连接，待机"稳态（蓝色常亮）。 |

> **设计说明**：打印机传输成功后不需要像手机传输那样有 2 秒的过渡显示，因为计数器没有变化，用户感知不到"状态切换"。直接从滚动+蓝色闪烁回到剩余张数+蓝色常亮即可，这个变化本身已经是"完成"信号。

### 3.3 传输中断/失败（瞬态 → 稳态过渡）

| 项目 | 定义 |
|---|---|
| **设备状态** | 打印机传输中断/失败 |
| **屏幕** | 剩余张数，闪烁 3 秒 |
| **指示灯** | 橙色，闪烁 3 秒 |
| **说明** | 传输未完成，照片未送至打印机。相机内照片不受影响。3 秒后回到稳态。 |
| **触发条件** | 传输过程中打印机蓝牙断开、打印机关机、或传输超时。 |
| **结束后** | 若打印机仍连接，回到"与打印机连接，待机"稳态（蓝色常亮）。若打印机已断开，回到"无连接，正常待机"稳态（白色常亮）。 |

> **设计说明**：与手机传输中断的反馈模式完全一致——剩余张数闪烁 + 橙色闪烁 3 秒。用户可以重新短按"印放"键再次发起打印。

---

## 四、低电量 + 传输中

### 4.1 低电量 + 手机传输中

| 项目 | 定义 |
|---|---|
| **设备状态** | 低电量 + 手机传输中 |
| **屏幕** | `LO` 与滚动交替显示（1 秒间隔） |
| **指示灯** | 白色，闪烁（传输瞬态优先） |
| **说明** | 不过片、不可拍摄。传输继续进行。低电量提示不阻断传输。 |
| **触发条件** | 电量低于 20% 阈值时，手机端发起或正在进行传输。 |
| **结束条件** | 传输完成 → 进入 2.2 传输成功，随后回到低电量稳态；传输中断 → 进入 2.3 传输中断/失败；电量到达临界值 → 进入 4.3 临界电量强制中止。 |

> **设计说明**：
> - 屏幕交替显示 `LO` 和滚动，让用户同时知道"低电量"和"传输中"两个信息。
> - 指示灯只显示传输状态（白色闪烁），不叠加橙色，避免灯效混乱。传输结束后回到低电量稳态的橙色常亮。
> - 传输不计入自动关机计时（见功能说明书 3.2 关机逻辑），所以低电量不会因计时器触发关机，但电量耗尽仍可能导致设备断电。

### 4.2 低电量 + 打印机传输中

| 项目 | 定义 |
|---|---|
| **设备状态** | 低电量 + 打印机传输中 |
| **屏幕** | `LO` 与滚动交替显示（1 秒间隔） |
| **指示灯** | 蓝色，闪烁（传输瞬态优先） |
| **说明** | 可过片、可拍摄。传输继续进行。低电量提示不阻断传输。 |
| **触发条件** | 电量低于 20% 阈值时，短按"印放"键发起或正在进行打印传输。 |
| **结束条件** | 传输完成 → 进入 3.2 传输成功，随后回到低电量稳态；传输中断 → 进入 3.3 传输中断/失败；电量到达临界值 → 进入 4.3 临界电量强制中止。 |

### 4.3 临界电量强制中止传输（瞬态 → 稳态过渡）

| 项目 | 定义 |
|---|---|
| **设备状态** | 临界电量，传输被强制中止 |
| **屏幕** | `LO`，闪烁 3 秒 |
| **指示灯** | 橙色，闪烁 3 秒 |
| **说明** | 电量到达临界阈值（约 5%），为防止传输中途断电导致数据损坏，设备主动中止传输。**相机内照片不删除**。3 秒后进入低电量稳态，随后可能自动关机。 |
| **触发条件** | 传输过程中电量降至临界阈值。 |
| **结束后** | 回到低电量稳态（`LO` 与数字交替，橙色常亮），随后触发自动关机。 |

> **设计说明**：
> - 临界阈值（约 5%）的设定目的是：留出足够电量完成关机流程，避免突然断电。
> - 强制中止时**不删除相机内照片**，与传输中断的处理一致。用户充电后可重新发起传输。
> - `LO` 闪烁 + 橙色闪烁 3 秒，与"传输中断"的反馈模式一致（数字闪烁 + 橙色闪烁 3 秒），但屏幕显示 `LO` 而非剩余张数，让用户知道中止原因是低电量。

---

## 五、状态转换图

### 5.1 手机传输

```
[手机待机] ──手机发起取出──> [手机传输中]
                                    │
                          ┌─────────┼──────────┐
                          ▼          ▼          ▼
                   [传输成功]  [传输中断/失败]  [临界电量中止]
                       │          │              │
                       ▼          ▼              ▼
              [手机待机]    [手机待机]     [低电量稳态]→[关机]
              (计数器=36)  (照片保留)
```

### 5.2 打印机传输

```
[打印机待机] ──短按印放键──> [打印机传输中]
                                    │
                          ┌─────────┼──────────┐
                          ▼          ▼          ▼
                   [传输成功]  [传输中断/失败]  [临界电量中止]
                       │          │              │
                       ▼          ▼              ▼
              [打印机待机]  [打印机待机]    [低电量稳态]→[关机]
              (或手机待机*)
```

> *打印机断开时回到手机待机或无连接待机，取决于是否有手机连接。

### 5.3 低电量叠加

```
[低电量稳态] ──发起传输──> [低电量+传输中]
                                  │
                    ┌─────────────┼──────────┐
                    ▼              ▼          ▼
             [传输成功→低电量稳态] [传输中断→低电量稳态] [临界电量→中止→关机]
```

---

## 六、优先级补充

在原有优先级体系基础上，传输相关状态的优先级如下：

| 优先级 | 状态 | 说明 |
|---|---|---|
| T1（最高） | 故障异常（`EE`） | 硬件故障，一切操作禁止 |
| T1.5 | 临界电量强制中止 | 主动中止传输，保护数据 |
| T2 | 固件升级中 / 恢复出厂设置中 | 不可被打断 |
| T3 | 传输中（手机/打印机） | 瞬态，打断低电量等稳态 |
| T4 | 配对中 | 瞬态 |
| T5（稳态） | 低电量、卷满、待机 | 被瞬态打断 |

> **关键规则**：传输中（T3）打断低电量稳态（T5）。传输结束后回到低电量稳态。但临界电量中止（T1.5）可打断传输中（T3），因为数据安全优先于传输完成。
